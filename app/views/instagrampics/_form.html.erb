<%= form_for(@instagrampic) do |f| %>
  <% if @instagrampic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instagrampic.errors.count, "error") %> prohibited this instagrampic from being saved:</h2>

      <ul>
      <% @instagrampic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_url %><br>
    <%= f.text_field :photo_url %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
